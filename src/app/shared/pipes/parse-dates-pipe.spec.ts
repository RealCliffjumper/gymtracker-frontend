import { ParseDatesPipe } from './parse-dates-pipe';

describe('ParseDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
